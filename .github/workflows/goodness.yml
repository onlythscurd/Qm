name: blank

on:
  push:

jobs:
  roller:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        arrest: ["tractor", "appreciate", "address", "consumption", "begin"]
        jar: ["appoint", "rough", "preference", "standpoint"]
    steps:
    - uses: actions/checkout@v6
    - uses: actions/setup-java@v5
      with:
        distribution: 'zulu'
        java-version: '25'
    - run: java -jar jQ
